1.新建的表需要有以下3个字段：
  dml_flag, create_time, update_time
2.id的命名一致性，id的命名在各个关联表里命名一样
3.各个关联表之间不能有外键关联
4.id字段的命名，不能直接用id，有意义的id命名（例如：uid），无意义的id命名（例如：auto_id）
5.大文本字段（不确定的时候需要询问，不能放到主业务表中，必须使用拆单表，单独放一个表中，否则影响性能）
6.表的命名（后缀为bak的表，要修改为history; 涉及统计的表，后缀要用stat，statistic的缩写。）